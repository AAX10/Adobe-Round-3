# E-002 — Schema Localization

## Mechanism
Knowledge Graph Geographic Bounding

## Concrete Claim

IF:
A web page representing an organization specifies an `address` property in its JSON-LD structured data but omits the `areaServed` (or a multi-region property) when offering broader services,

THEN:
An AI assistant is more likely to restrict the source's relevance strictly to the user's immediate physical location, because knowledge graphs and entity extraction systems use structured address data as a strong geographic bounding box unless explicitly widened.

## Evidence Needed

### To Confirm
- Fetch and parse the site's JSON-LD scripts.
- Identify `Organization` or `LocalBusiness` schemas.
- Check for the presence of the `address` property.
- Check for the absence of `areaServed` or equivalent global/multi-regional properties.

### To Reject or Narrow
- The schema contains `areaServed`, `location`, or explicitly defined service footprints.
- The site does not use `Organization` or `LocalBusiness` JSON-LD schemas.

## Observable Signal
Valid JSON-LD schema parsed containing an `address` property but no `areaServed` property.

## Automated Decision Boundary
A finding is generated when:
1. `Organization` or `LocalBusiness` schema is found.
2. `address` property exists.
3. `areaServed` property does not exist.

## Potential Severity

**High**: Strict bounding box constraints drastically reduce relevance for broad queries.

## Potential Remediation
Explicitly define the regional or global footprint of the service in the structured data.
- **Where to change:** Within the `Organization` or `LocalBusiness` JSON-LD `<script type="application/ld+json">` tag.
- **Example Fix:** 
  ```json
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": "Acme Tech Services",
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "Austin",
      "addressRegion": "TX"
    },
    "areaServed": [
      {
        "@type": "State",
        "name": "Texas"
      },
      {
        "@type": "Country",
        "name": "United States"
      }
    ]
  }
  ```
- **Why this works:** It provides a deterministic, machine-readable footprint expansion, overriding the default assumption that the business only exists at its physical coordinates.

## Current Evidence

**SOURCE-DERIVED FACT**: The Schema.org specification for `Organization` and `LocalBusiness` explicitly defines the `areaServed` property to denote "The geographic area where a service or offered item is provided" as distinct from the `address` property which indicates the physical location of the business. Google Search Central explicitly highlights the use of `areaServed` for service-area businesses.

**OUR INFERENCE**: If an AI system ingests structured data to populate its knowledge graph, the presence of `address` without `areaServed` structurally constrains the entity to its physical coordinate bounding box. Because modern AI assistants frequently leverage search engine APIs and Knowledge Graphs as RAG backends, failing to supply `areaServed` translates directly to limited AI discoverability outside the immediate physical locale.

## Counterexamples

1. **No schema present** → NOT a finding. If there is no `Organization` or `LocalBusiness` schema, the risk is structural absence, not strict localization.

## References

1. **Schema.org - Organization**
   - Organization: Schema.org
   - URL: https://schema.org/Organization
   - Mechanism supported: Knowledge Graph Parsing
   - Claim supported: Organizations have distinct properties for physical location (`address`) versus service footprint (`areaServed`).

2. **Local Business structured data**
   - Organization: Google Search Central
   - URL: https://developers.google.com/search/docs/appearance/structured-data/local-business
   - Mechanism supported: Entity Graph Bounding
   - Claim supported: Search engines utilize structured geographic properties to determine the valid context and reach of a business entity.

## Known Limitations
- Does not check if the business actually *needs* `areaServed` (a pure local restaurant doesn't). The finding is structural risk, assuming the business intends broader reach.

## Hypothesis Quality Score

| Criterion | Score (1-5) | Rationale |
|-----------|-------------|-----------|
| Mechanistic plausibility | 5 | Knowledge graphs strictly obey schema declarations |
| Evidence quality | 5 | Schema.org is the absolute authority |
| Observability | 5 | JSON-LD is deterministic |
| Automation feasibility | 5 | Trivial to parse |
| Cross-site generalization | 5 | All sites using structured data follow this schema |
| False-positive resistance | 5 | Purely structural check |
| Actionability | 5 | Trivial to fix in JSON |
| Unseen-site applicability | 5 | Yes |
| Runtime feasibility | 5 | Very fast |
| **Average** | **5.0** | |

## Experiment Design

1. **Condition tested**: `address` exists, `areaServed` does not.
2. **Expected bad outcome**: Source not retrieved for out-of-region queries.
3. **Ground truth**: Business serves a wider area.
4. **Supports hypothesis**: Valid JSON-LD found missing `areaServed`.
5. **Kills hypothesis**: AI ignores JSON-LD completely for spatial bounding.
6. **Obvious false positives**: Purely local brick-and-mortar.
7. **Automatable**: Yes.
8. **Works on unseen site**: Yes.
9. **Evidence emitted**: JSON-LD path and boolean existence checks.

## Classification
CORE
